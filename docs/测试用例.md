# TaskFlow AI 测试用例文档

> 测试框架: JUnit 5 + Mockito + Spring Boot Test
> 测试总数: 92 | 全部通过
> 更新日期: 2026-05-22

---

## 一、单元测试用例

### 1.1 ToolManager 工具管理器 (5 cases)

| # | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|---|---------|---------|---------|---------|
| 1.1.1 | 注册并获取工具 | ToolManager 已初始化 | 注册 StubTool("weather")，调用 getTool("weather") | 返回非 null 的 Tool 实例，name 为 "weather" |
| 1.1.2 | 列出所有已注册工具 | 注册了 2 个 StubTool | 调用 listAvailableTools() | 返回包含 2 个元素的 List |
| 1.1.3 | 获取工具名称列表 | 注册了 2 个 StubTool("weather", "search") | 调用 getToolNames() | 返回 ["weather", "search"] |
| 1.1.4 | 获取不存在的工具 | 未注册任何工具 | 调用 getTool("nonexistent") | 抛出 IllegalArgumentException，消息含 "Tool not found" |
| 1.1.5 | 获取所有工具为 Map | 注册了 1 个 StubTool("weather") | 调用 getAllTools() | 返回 size=1 的 Map，包含 key "weather" |

### 1.2 ExecutionContext 执行上下文 (10 cases)

| # | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|---|---------|---------|---------|---------|
| 1.2.1 | 创建执行上下文 | 无 | new ExecutionContext("task-1", "测试任务", 10, 30) | traceId 不为空，status=PENDING，retryCount=0 |
| 1.2.2 | traceId 自动生成 | 无 | 创建 ExecutionContext | traceId 为 36 字符 UUID 格式 |
| 1.2.3 | 超时判断 - 未超时 | 创建 ctx，刚创建 | 调用 isExpired() | 返回 false |
| 1.2.4 | 超时判断 - 已超时 | 创建 ctx，expiresAt 设为过去时间 | 调用 isExpired() | 返回 true |
| 1.2.5 | 增加重试次数 | ctx.retryCount=0 | 调用 incrementRetryCount() | retryCount 变为 1 |
| 1.2.6 | 重试次数超限 | ctx.retryCount=2，maxRetries=2 | 调用 canRetry() | 返回 false |
| 1.2.7 | 最大步数限制 | ctx.maxSteps=5 | 执行 5 步后调用 canProceed() | 返回 false |
| 1.2.8 | 状态流转 PENDING→RUNNING | ctx.status=PENDING | 调用 markRunning() | status 变为 RUNNING |
| 1.2.9 | 状态流转 RUNNING→COMPLETED | ctx.status=RUNNING | 调用 markCompleted() | status 变为 COMPLETED |
| 1.2.10 | 设置最后错误信息 | ctx 正常运行中 | 调用 setLastError("some error") | lastError 不为 null，内容正确 |

### 1.3 CalculatorTool 计算器工具 (10 cases)

| # | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|---|---------|---------|---------|---------|
| 1.3.1 | 获取工具名称 | CalculatorTool 已创建 | 调用 getName() | 返回 "calculator" |
| 1.3.2 | 获取工具描述 | 同上 | 调用 getDescription() | 返回非空字符串 |
| 1.3.3 | 获取输入 Schema | 同上 | 调用 getInputSchema() | 包含 "expression" 参数，required=true |
| 1.3.4 | 简单加法计算 | 同上 | execute({expression: "2 + 3"}) | success=true，data="5.0" |
| 1.3.5 | 乘法计算 | 同上 | execute({expression: "4 * 5"}) | success=true，data="20.0" |
| 1.3.6 | 复合运算优先级 | 同上 | execute({expression: "2 + 3 * 4"}) | success=true，data="14.0"（乘法优先）|
| 1.3.7 | 除法运算 | 同上 | execute({expression: "10 / 3"}) | success=true，data 不为 null |
| 1.3.8 | 无效表达式 | 同上 | execute({expression: "2 + abc"}) | success=false，errorMessage 含错误信息 |
| 1.3.9 | 执行时间记录 | 同上 | execute({expression: "1 + 1"}) | executionTimeMs >= 0 |
| 1.3.10 | 默认不重试 | 同上 | 调用 getMaxRetries() | 返回 0 |

### 1.4 WeatherTool 天气查询工具 (6 cases)

| # | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|---|---------|---------|---------|---------|
| 1.4.1 | 获取工具名称 | WeatherTool 已创建 | 调用 getName() | 返回 "weather" |
| 1.4.2 | 获取工具描述 | 同上 | 调用 getDescription() | 返回非空字符串 |
| 1.4.3 | 获取输入 Schema | 同上 | 调用 getInputSchema() | 包含 "city" 参数，required=true |
| 1.4.4 | 空城市名拒绝 | 同上 | execute({city: ""}) | success=false，errorMessage 含 "不能为空" |
| 1.4.5 | 正常查询（使用 MockWebServer） | MockWebServer 返回天气 JSON | execute({city: "Beijing"}) | success=true，data 包含天气信息 |
| 1.4.6 | API 返回错误状态码 | MockWebServer 返回 500 | execute({city: "Beijing"}) | success=false，errorMessage 含 "HTTP 500" |

### 1.5 SearchTool 搜索工具（Tavily API, POST + JSON） (5 cases)

| # | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|---|---------|---------|---------|---------|
| 1.5.1 | 获取工具名称 | SearchTool 已创建 | 调用 getName() | 返回 "search" |
| 1.5.2 | 获取输入 Schema | 同上 | 调用 getInputSchema() | 包含 "query"(required) 和 "resultCount"(optional) |
| 1.5.3 | 空查询词拒绝 | 同上 | execute({query: ""}) | success=false，errorMessage 含 "不能为空" |
| 1.5.4 | 正常搜索（Tavily JSON 响应） | MockWebServer 返回 Tavily 格式搜索结果 | execute({query: "Java", resultCount: 3}) | success=true，data 包含 results 数组 |
| 1.5.5 | API 返回错误状态码 | MockWebServer 返回 401 | execute({query: "Java"}) | success=false，errorMessage 含 "HTTP 401" |

### 1.6 LLMClient 大模型客户端 (5 cases)

| # | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|---|---------|---------|---------|---------|
| 1.6.1 | 正常调用返回响应 | MockWebServer 返回 LLM JSON | llmClient.chat("你好") | 返回包含 choices 的非空字符串 |
| 1.6.2 | 请求体包含 model 字段 | MockWebServer 记录请求 | llmClient.chat("测试") | 请求体 JSON 中 model 字段匹配配置 |
| 1.6.3 | API 返回错误状态码 | MockWebServer 返回 401 | llmClient.chat("测试") | 抛出 IOException |
| 1.6.4 | 连接超时处理 | MockWebServer 延迟响应 | llmClient.chat("测试") | 抛出 IOException（超时） |
| 1.6.5 | 空响应体处理 | MockWebServer 返回空 body | llmClient.chat("测试") | 抛出 IOException |

### 1.7 TaskAgentImpl ReAct 循环引擎 (6 cases)

| # | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|---|---------|---------|---------|---------|
| 1.7.1 | 单步任务执行成功 | LLM 返回 1 步计划(calculator,2+3) | agent.execute("计算 2+3") | status=COMPLETED，steps=1 |
| 1.7.2 | 多步任务执行成功 | LLM 返回 2 步计划 | agent.execute("多步任务") | status=COMPLETED，steps=2 |
| 1.7.3 | 工具调用失败处理 | LLM 返回使用不存在的工具 | agent.execute("无效任务") | status=FAILED，含错误信息 |
| 1.7.4 | 最大步数超限 | maxSteps=1，LLM 返回 3 步 | agent.execute("复杂任务") | status=FAILED/TIMEOUT，steps<=1 |
| 1.7.5 | LLM 返回无效 JSON | LLM 返回纯文本 | agent.execute("测试") | status=FAILED |
| 1.7.6 | 结果合成正确 | 多步任务执行完成 | agent.execute("测试") | result.summary 非空，包含所有步骤结果 |

### 1.8 DatabaseTool 数据库查询工具 (10 cases)

| # | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|---|---------|---------|---------|---------|
| 1.8.1 | 获取工具名称 | H2 内存库已建表 users(3行) | 调用 getName() | 返回 "database" |
| 1.8.2 | 获取工具描述 | 同上 | 调用 getDescription() | 返回非空字符串 |
| 1.8.3 | 获取输入 Schema | 同上 | 调用 getInputSchema() | sql(required)，params(optional) |
| 1.8.4 | SELECT 全表查询 | users 表有 3 行 | execute({sql: "SELECT * FROM users"}) | success=true，rowCount=3，rows.size=3 |
| 1.8.5 | WHERE 条件查询 | 同上 | execute({sql: "SELECT * FROM users WHERE age > ?", params: [28]}) | success=true，rowCount=2 |
| 1.8.6 | 多参数查询 | 同上 | execute({sql: "...WHERE age > ? AND name LIKE ?", params: [20, "A%"]}) | success=true，rowCount=1 |
| 1.8.7 | 拒绝 DROP 语句 | 同上 | execute({sql: "DROP TABLE users"}) | success=false，含 "仅允许执行 SELECT" |
| 1.8.8 | 拒绝 INSERT 语句 | 同上 | execute({sql: "INSERT INTO users VALUES (4, 'Dave', 40)"}) | success=false，含 "仅允许执行 SELECT" |
| 1.8.9 | 空 SQL 拒绝 | 同上 | execute({sql: ""}) | success=false，含 "不能为空" |
| 1.8.10 | 无效表名处理 | 同上 | execute({sql: "SELECT * FROM nonexistent"}) | success=false，含 SQL 错误信息 |

### 1.9 FileOperationTool 文件操作工具 (10 cases)

| # | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|---|---------|---------|---------|---------|
| 1.9.1 | 获取工具名称 | 使用 @TempDir 白名单目录 | 调用 getName() | 返回 "file_operation" |
| 1.9.2 | 获取工具描述 | 同上 | 调用 getDescription() | 返回非空字符串 |
| 1.9.3 | 获取输入 Schema | 同上 | 调用 getInputSchema() | 含 operation(required)、path(required)、content |
| 1.9.4 | 读取已存在文件 | 创建 test.txt，内容 "Hello, World!" | execute({op: "read", path: "test.txt"}) | success=true，data="Hello, World!" |
| 1.9.5 | 写入新文件 | 白名单目录为空 | execute({op: "write", path: "output.txt", content: "new content"}) | success=true，可再次 read 验证内容 |
| 1.9.6 | 列出目录内容 | 目录含 a.txt, b.txt, subdir/ | execute({op: "list", path: "."}) | success=true，data 含 "a.txt","b.txt","subdir" |
| 1.9.7 | 删除文件 | 创建 to_delete.txt | execute({op: "delete", path: "to_delete.txt"}) | success=true，文件不存在 |
| 1.9.8 | 拒绝路径穿越 | 白名单为 /tmp/xxx | execute({op: "read", path: "../etc/passwd"}) | success=false，含 "白名单" |
| 1.9.9 | 空操作拒绝 | 同上 | execute({op: "", path: "test.txt"}) | success=false，含 "不能为空" |
| 1.9.10 | 未知操作拒绝 | 同上 | execute({op: "execute", path: "test.txt"}) | success=false，含 "不支持" |

### 1.10 JsonTool JSON 处理工具 (12 cases)

| # | 用例名称 | 前置条件 | 测试步骤 | 预期结果 |
|---|---------|---------|---------|---------|
| 1.10.1 | 获取工具名称 | JsonTool 已创建 | 调用 getName() | 返回 "json" |
| 1.10.2 | 获取工具描述 | 同上 | 调用 getDescription() | 返回非空字符串 |
| 1.10.3 | 获取输入 Schema | 同上 | 调用 getInputSchema() | operation(required)、json(required)、expression |
| 1.10.4 | 解析 JSON 对象 | 合法 JSON 对象 | execute({op: "parse", json: '{"name":"Alice","age":30}'}) | valid=true，type=OBJECT，fieldCount=2 |
| 1.10.5 | 解析 JSON 数组 | 合法 JSON 数组 | execute({op: "parse", json: "[1,2,3,4,5]"}) | valid=true，type=ARRAY，elementCount=5 |
| 1.10.6 | 拒绝无效 JSON | 非法 JSON 字符串 | execute({op: "parse", json: "{name: invalid}"}) | success=false，含 "JSON" |
| 1.10.7 | 点分隔路径查询 | 嵌套 JSON 对象 | execute({op: "query", json: ..., expression: "user.profile.name"}) | success=true，data="Alice" |
| 1.10.8 | 从数组提取属性 | JSON 数组 | execute({op: "query", json: [...], expression: "name"}) | success=true，data=["Alice","Bob"] |
| 1.10.9 | 数组索引访问 | JSON 数组 | execute({op: "query", json: [...], expression: "1.name"}) | success=true，data="Bob" |
| 1.10.10 | 不存在的路径 | JSON 对象无该字段 | execute({op: "query", json: ..., expression: "address.city"}) | success=false，含 "路径未找到" |
| 1.10.11 | 字段重命名转换 | JSON 对象含 first_name,last_name | execute({op: "transform", json: ..., expression: "first_name:firstName,last_name:lastName"}) | data.firstName="Alice"，data.lastName="Smith" |
| 1.10.12 | 空操作拒绝 | 同上 | execute({op: "", json: "{}"}) | success=false，含 "不能为空" |

---

## 二、API 测试用例

### 2.1 TaskAgentController API 端点 (7 cases)

| # | 用例名称 | 请求方法 | 端点 | 请求体/参数 | 预期状态码 | 预期响应 |
|---|---------|---------|------|-----------|-----------|---------|
| 2.1.1 | 提交任务执行 | POST | /api/v1/agent/execute | `{"task":"计算 2+3","timeout":30,"maxSteps":5}` | 202 | taskId(UUID), status, executionTimeMs |
| 2.1.2 | 查询任务状态 | GET | /api/v1/agent/status/{taskId} | 已存在的 taskId | 200 | taskId, status, currentStep, createdAt |
| 2.1.3 | 查询任务详情 | GET | /api/v1/agent/{taskId}/details | 已存在的 taskId | 200 | taskId, steps[], result, executionTimeMs |
| 2.1.4 | 查询执行历史 | GET | /api/v1/agent/history?page=0&size=20 | 无 | 200 | content[], totalElements, totalPages |
| 2.1.5 | 查询不存在的任务 | GET | /api/v1/agent/status/{uuid} | 不存在的 UUID | 404 | errorCode, message 含 "not found" |
| 2.1.6 | 提交空任务描述 | POST | /api/v1/agent/execute | `{"task":""}` | 400 | errorCode, message 含校验信息 |
| 2.1.7 | 查询工具列表 | GET | /api/v1/tools | 无 | 200 | JSON 数组，包含已注册工具信息 |

### 2.2 集成测试 (6 cases)

| # | 用例名称 | 测试场景 | 验证点 |
|---|---------|---------|--------|
| 2.2.1 | 执行任务并返回结果 | POST /api/v1/agent/execute，LLM 返回 1 步计划 | 202 + taskId + status + executionTimeMs |
| 2.2.2 | 执行后查询状态 | 先 execute 获取 taskId，再 GET status/{id} | 200 + taskId 匹配 |
| 2.2.3 | 列出可用工具 | GET /api/v1/tools | 200 + 非空数组，第一项为 calculator |
| 2.2.4 | 查询任务详情 | 先 execute，再 GET details/{id} | 200 + taskId 匹配 + steps 不为空 |
| 2.2.5 | 查询执行历史 | GET /api/v1/agent/history | 200 + content 数组（可为空） |
| 2.2.6 | 复杂多步任务 | execute 带 timeout=60, maxSteps=10 | 202 + taskId 存在 |

---

## 三、异常场景测试用例

### 3.1 超时场景

| # | 用例名称 | 触发方式 | 预期行为 |
|---|---------|---------|---------|
| 3.1.1 | 任务执行超时 | 设置 timeout=1s，执行耗时任务 | status=TIMEOUT，错误码 TASK_TIMEOUT |
| 3.1.2 | 工具调用超时 | 工具 getTimeoutMs()=1ms，执行慢操作 | ToolResult.failure，含超时信息 |

### 3.2 工具失败场景

| # | 用例名称 | 触发方式 | 预期行为 |
|---|---------|---------|---------|
| 3.2.1 | 工具不存在 | LLM 返回的 tool name 未注册 | status=FAILED，含 "Tool not found" |
| 3.2.2 | 工具参数不匹配 | 传入的参数与 Schema 不符 | 参数缺失时报错 |
| 3.2.3 | 工具内部异常 | CalculatorTool 收到除零表达式 | success=false，含错误详情 |

### 3.3 LLM 错误场景

| # | 用例名称 | 触发方式 | 预期行为 |
|---|---------|---------|---------|
| 3.3.1 | LLM API 不可达 | API Base URL 指向无效地址 | 抛出 IOException，任务标记 FAILED |
| 3.3.2 | LLM 返回无效 JSON | MockWebServer 返回纯文本 | status=FAILED，含解析错误信息 |
| 3.3.3 | LLM 返回格式不符 | MockWebServer 返回不含 steps 的 JSON | status=FAILED |

### 3.4 鉴权失败场景

| # | 用例名称 | 触发方式 | 预期行为 |
|---|---------|---------|---------|
| 3.4.1 | 缺少 API Key | 不传 X-API-Key 头（auth.enabled=true） | 401，错误码 UNAUTHORIZED |
| 3.4.2 | 无效 API Key | 传入错误的 API Key | 401，错误码 INVALID_API_KEY |
| 3.4.3 | API Key 为空 | X-API-Key 头传空字符串 | 401，错误码 UNAUTHORIZED |

### 3.5 幂等性场景

| # | 用例名称 | 触发方式 | 预期行为 |
|---|---------|---------|---------|
| 3.5.1 | 重复请求 | 相同 X-Idempotency-Key 的两次 POST | 第二次返回 409，含 "duplicate" |
| 3.5.2 | 不同请求 | 不同 X-Idempotency-Key 的两次 POST | 两次均正常处理 |

---

## 四、前端 UI 测试用例

### 4.1 页面加载

| # | 用例名称 | 操作步骤 | 预期结果 |
|---|---------|---------|---------|
| 4.1.1 | 管理界面加载 | 访问 http://localhost:8080/ | 显示 SPA 管理界面，含导航栏 |
| 4.1.2 | 深色模式切换 | 点击深色模式按钮 | 页面切换为深色主题，再次点击恢复 |
| 4.1.3 | 响应式布局 | 缩小浏览器窗口至 768px 以下 | 布局自适应，表格横向滚动 |

### 4.2 任务提交

| # | 用例名称 | 操作步骤 | 预期结果 |
|---|---------|---------|---------|
| 4.2.1 | 提交简单任务 | 输入 "计算 2+3"，点击提交 | 显示 taskId，状态更新 |
| 4.2.2 | 提交空任务 | 不输入任何内容，点击提交 | 前端提示输入内容 |
| 4.2.3 | 设置自定义参数 | 修改 timeout/maxSteps 值后提交 | 使用自定义参数执行 |

### 4.3 执行历史

| # | 用例名称 | 操作步骤 | 预期结果 |
|---|---------|---------|---------|
| 4.3.1 | 查看历史列表 | 切换到"历史"标签 | 显示已完成任务的表格 |
| 4.3.2 | 查看步骤详情 | 点击历史中的某条记录 | 展开显示步骤详情 |
| 4.3.3 | 空历史展示 | 新环境无历史记录 | 显示空状态提示 |

### 4.4 工具列表与健康

| # | 用例名称 | 操作步骤 | 预期结果 |
|---|---------|---------|---------|
| 4.4.1 | 查看工具列表 | 切换到"工具"标签 | 显示已注册的 6 个工具 |
| 4.4.2 | 查看健康状态 | 查看健康指示器 | 显示 UP 状态 |

---

## 五、集成测试用例（端到端）

### 5.1 完整任务执行流程

| # | 步骤 | 操作 | 验证 |
|---|------|------|------|
| 5.1.1 | 提交任务 | POST /api/v1/agent/execute | 202 + taskId |
| 5.1.2 | 轮询状态 | GET /api/v1/agent/status/{taskId} | 状态从 PENDING→RUNNING→COMPLETED |
| 5.1.3 | 获取详情 | GET /api/v1/agent/{taskId}/details | steps 数组非空，每步含 tool/params/result |
| 5.1.4 | 确认历史 | GET /api/v1/agent/history | 新任务出现在历史列表中 |

### 5.2 多工具协作场景

| # | 描述 | 涉及工具 | 验证 |
|---|------|---------|------|
| 5.2.1 | 计算 + 存储 | calculator → database | 计算结果可存入数据库查询 |
| 5.2.2 | 搜索 + JSON 处理 | search → json | 搜索结果可用 JSON query 提取字段 |
| 5.2.3 | 文件读写 + JSON 转换 | file_operation → json | 读取 JSON 文件后可用 json.parse 验证 |

---

## 六、测试统计

| 分类 | 用例数 | 文件 |
|------|--------|------|
| 单元测试 - ToolManager | 5 | ToolManagerTest.java |
| 单元测试 - ExecutionContext | 10 | ExecutionContextTest.java |
| 单元测试 - CalculatorTool | 10 | CalculatorToolTest.java |
| 单元测试 - WeatherTool | 6 | WeatherToolTest.java |
| 单元测试 - SearchTool | 5 | SearchToolTest.java |
| 单元测试 - LLMClient | 5 | LLMClientTest.java |
| 单元测试 - TaskAgentImpl | 6 | TaskAgentTest.java |
| 单元测试 - DatabaseTool | 10 | DatabaseToolTest.java |
| 单元测试 - FileOperationTool | 10 | FileOperationToolTest.java |
| 单元测试 - JsonTool | 12 | JsonToolTest.java |
| API 测试 - TaskAgentController | 7 | TaskAgentControllerTest.java |
| 集成测试 | 6 | TaskAgentIntegrationTest.java |
| **总计** | **92** | |
